# SCL-402 Família Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar a vertical editorial pública Família sem alterar catálogo ou quiz.

**Architecture:** Estender a configuração estática existente de experiências e reutilizar o template dinâmico atual; os assets temporários ficam isolados em `public/images/site/home` para substituição posterior.

**Tech Stack:** Next.js 16.3, React 19, TypeScript, Vitest e assets WebP.

## Global Constraints

- Não inventar preço, pacote, duração ou disponibilidade.
- Não alterar catálogo persistido nem quiz.
- A rota deve ter metadata, CTA contextual e 404 para slug inválido.

### Task 1: Conteúdo, assets e cobertura da vertical

**Files:** Modify `lib/site/experiences.ts`; create assets `public/images/site/home/familia-*.webp`; create/modify `tests/app/experiences-page.test.tsx` and `tests/lib/site/experiences.test.ts`.

- [ ] Escrever testes que esperam `familia` em `generateStaticParams`, metadata própria, CTA WhatsApp contextual e `notFound` para slug desconhecido.
- [ ] Rodar `npx vitest run tests/app/experiences-page.test.tsx tests/lib/site/experiences.test.ts --reporter=verbose --maxWorkers=1`; esperar falha.
- [ ] Adicionar a entrada `familia` com título, copy editorial, FAQ e referências aos assets temporários; gerar os WebPs temporários e manter todos os campos de preço/pacote ausentes.
- [ ] Rodar os testes, `npm run lint`, `npm run typecheck` e `git diff --check`; esperar PASS.
- [ ] Commit: `feat: add family experience vertical`.
